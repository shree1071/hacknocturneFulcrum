/**
 * DEPRECATED: The frontend no longer calls InsForge directly.
 * All database operations now go through the FastAPI backend (api.ts).
 * This file is kept as a placeholder to avoid import errors during migration.
 * 
 * Remove any remaining imports of `insforge` from components and replace
 * them with the corresponding methods from `@/lib/api`.
 */

export const insforge = null;
